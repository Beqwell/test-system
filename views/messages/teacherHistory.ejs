<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Sent Messages</title>
  <style>
    body {
      font-family: Arial, sans-serif;
      padding: 20px;
    }
    li {
      margin-bottom: 15px;
      line-height: 1.5;
    }
    hr {
      margin-top: 10px;
    }
    a {
      text-decoration: none;
      color: #5b21b6;
    }
    a:hover {
      text-decoration: underline;
    }
  </style>
</head>
<body>
  <h1>📨 Sent Messages — "<%= courseName %>"</h1>


  <ul>
    <% messages.forEach(msg => { %>
      <li>
        <div><strong>To:</strong>
          <% if (msg.student_id) { %>
            <%= msg.student_name %>
          <% } else { %>
            All Students
          <% } %>
        </div>
        <div><strong>Message:</strong> <%= msg.content %></div>
        <div><strong>Time:</strong> <%= new Date(msg.created_at).toLocaleString() %></div>
        <% if (msg.student_id) { %>
          <div>Status: <%= msg.is_read ? '✅ Read' : '🕒 Not Read' %></div>
        <% } %>
        <hr>
      </li>
    <% }) %>
  </ul>

  <br>
  <a href="/dashboard">← Back</a>
</body>
</html>
